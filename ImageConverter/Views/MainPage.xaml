<Page x:Class="ImageConverter.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:common="using:ImageConverter.Common"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:ImageConverter"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.Background>
            <common:BlurBrush />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  TITLE BAR  -->
        <TextBlock FontSize="12" Text="Image Converter for Windows 10" TextLineBounds="Tight" />

        <!--  OUTPUT ROW  -->
        <Grid Grid.Row="1" Margin="0 35 0 15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Header="Output Directory" IsEnabled="False" Text="{x:Bind ViewModel.ExportFolder.Path, FallbackValue='', TargetNullValue=''}" />

            <Button Grid.Column="1"
                    Margin="10 0 30 0"
                    VerticalAlignment="Bottom"
                    Click="{x:Bind ViewModel.DestinationClick}"
                    Content="..." />

            <ComboBox Grid.Column="2" Margin="10 0 0 0" Header="Output Format" />

            <ComboBox Grid.Column="3" Margin="10 0 0 0" Header="Output Quality" />

        </Grid>

        <!--  INPUT ROW  -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Margin="0 0 10 0"
                    Click="{x:Bind ViewModel.AddFilesClick}"
                    Content="Add Files" />
            <Button Grid.Column="1" Margin="0 0 10 0" Content="Remove Files" />
            <Button Grid.Column="2" Margin="0 0 10 0" Content="Clear Files" />

            <Button Grid.Column="5" Click="{x:Bind ViewModel.ConvertClick}" Content="Convert" />

        </Grid>

        <!--  FILES VIEW  -->
        <ListView Grid.Row="3" ItemsSource="{x:Bind ViewModel.FileList}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="common:ImageViewModel">
                    <Grid>
                        <TextBlock Text="{x:Bind File.DisplayName}" ToolTipService.ToolTip="{x:Bind File.Path}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
